
platform: linux
image_resource:
  type: registry-image
  source: { repository: busybox }

inputs:
- name: bucc-aws-repo

outputs:
- name: deployment-stuff

run:
  path: sh
  args:
  - -exc
  - |
    echo "applications: [ name: silly-app ]" > deployment-stuff/manifest.yml